## Facts
- `pi-package/extensions/model-aliases/config.ts` exposes async resolver `resolveModelSettingsWithAliases` and sync resolver `resolveModelSettingsWithAliasesSync`.
- Both resolvers return direct provider/model IDs unchanged.
- Alias IDs resolve to real target IDs; alias thinking applies only when caller thinking is absent (config.ts:76-84).
- Unknown aliases return a clear runtime issue (config.ts:68).
- Invalid JSON returns a parse issue (config.ts:169-173).
- Config loading delegated to shared helpers in agent-suite-storage.ts:76 and :89.
- Suite constant SUITE_CONFIG_FILE = config.json (agent-suite-storage.ts:17).
- Test fixtures validate suite-owned path model-aliases/config.json for both resolvers, explicit-thinking precedence, unknown alias, and malformed JSON (config.test.ts:29-128).
- Production imports confirmed in 7 files: auxiliary-llm.ts:9, tool-result-summary.ts:15, custom-compaction/index.ts:39, convene-council/runtime.ts:6, main-agent-selection/index.ts:42, run-subagent/agent-policy.ts:20, workflow/model-runtime.ts:12.

## Evidence gaps
- Malformed-JSON path is asserted only via the async resolver; no equivalent sync malformed-JSON assertion exists.
- Parser validation branches for invalid alias target shapes are not fully evidenced.

## Conclusion
model-aliases is a config module consumed as shared resolver library. Implementation is correct for observed behavior. Only actionable gap: missing sync malformed-JSON test coverage.

## Open Questions
- Whether malformed-JSON sync coverage was intentionally omitted or missed.

## Workflow status
Final stage generate_report completed. No repository files modified.