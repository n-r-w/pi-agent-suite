Extract only durable project knowledge discovered during the session.

Return concise Markdown that records critical changes, obstacles, decisions, and information useful to future sessions. Do not retell the session. Return exactly `NOT_FOUND` when no durable knowledge was discovered.
