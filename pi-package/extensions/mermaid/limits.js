/** Maximum source lines accepted for one Mermaid block. */
export const MAX_SOURCE_LINES = 400;
/** Maximum UTF-16 code units accepted for one Mermaid block. */
export const MAX_SOURCE_CHARACTERS = 20_000;
/** Maximum lines accepted for one rendered variant. */
export const MAX_VARIANT_LINES = 1_000;
/** Maximum UTF-16 code units accepted for one rendered variant. */
export const MAX_VARIANT_CHARACTERS = 100_000;
/** Maximum diagnostic characters accepted from runtime or persisted data. */
export const MAX_EXPLANATION_CHARACTERS = 500;
